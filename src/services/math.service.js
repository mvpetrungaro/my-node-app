export default {
    async add(a, b) {
        return a + b
    },

    async sub(a, b) {
        return a - b
    },

    async mul(a, b) {
        return a * b
    },

    async div(a, b) {
        return a / b
    }
}
